name: Bach.java
on: push
jobs:
  linux:
    name: Linux
    runs-on: ubuntu-latest
    container: openjdk:13
    steps:
    - uses: actions/checkout@v1
    - name: 'Build Bach.java'
      run: ./build.jsh
